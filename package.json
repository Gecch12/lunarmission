{
  "name": "perilune-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Private browser-based cooperative lunar mission game for 1-6 players.",
  "scripts": {
    "dev": "node --watch server/src/server.js",
    "start": "node server/src/server.js",
    "check": "node --check server/src/server.js && node --check client/scene.js && node --check client/app.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
